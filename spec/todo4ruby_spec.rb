require "spec_helper"

describe Todo4ruby do
  it "has a version number" do
    expect(Todo4ruby::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
